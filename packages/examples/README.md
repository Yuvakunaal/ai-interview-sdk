# examples

Runnable example apps land here in Phase 8, including the end-to-end example
that satisfies the 5-step Success Criteria from the product spec: install →
API key → questions + rubric → `<InterviewWidget />` → run.
