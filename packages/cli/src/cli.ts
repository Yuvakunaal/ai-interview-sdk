#!/usr/bin/env node
import { CLI_PACKAGE_NAME, CLI_PACKAGE_VERSION } from './index.js';

console.log(`${CLI_PACKAGE_NAME}@${CLI_PACKAGE_VERSION}`);
console.log('Commands (init, simulate, bias-harness) land in Phase 6 of the build.');
